# Environment Variables for Face Recognition Backend

# === REQUIRED FOR PRODUCTION ===

# MongoDB Atlas Connection String (Get from https://cloud.mongodb.com)
# Example: mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=your_mongodb_connection_string_here

# Frontend URL (your deployed frontend URL)
FRONTEND_URL=https://your-site.netlify.app

# Node Environment
NODE_ENV=production

# Port (auto-assigned by most platforms, leave as 3000 for local)
PORT=3000

# === OPTIONAL ===

# Alternative MongoDB variable name (some platforms use this)
MONGO_URI=your_mongodb_connection_string_here

# === LOCAL DEVELOPMENT ===

# For local development, you can leave MONGODB_URI empty
# The server will automatically use JSON file database as fallback

# Just set:
# NODE_ENV=development
# PORT=3000
